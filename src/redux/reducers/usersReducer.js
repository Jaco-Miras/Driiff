const INITIAL_STATE = {
    users: {},
    viewedProfile: null,
    onlineUsers: [],
};

export default (state = INITIAL_STATE, action) => {
    switch (action.type) {

        default:
            return state;
    }
} 