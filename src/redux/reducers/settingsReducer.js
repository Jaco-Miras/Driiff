
const INITIAL_STATE = {
    companySettings: null,
    userSettings: null
};

export default (state = INITIAL_STATE, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
} 