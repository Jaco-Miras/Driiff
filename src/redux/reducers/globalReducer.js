
const INITIAL_STATE = {
    recipients: [],
    isBrowserActive: true,
    modals: []
};

export default (state = INITIAL_STATE, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
} 