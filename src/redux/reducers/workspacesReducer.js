const INITIAL_STATE = {
    _global: {
        user: {},
    },
    workspaces: {},
};

export default (state = INITIAL_STATE, action) => {
    switch (action.type) {

        default:
            return state;
    }
} 